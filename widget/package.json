{
    "name": "@philiprehberger/foyer-widget",
    "private": true,
    "version": "0.1.0",
    "description": "Foyer embedded chat widget. Preact + Shadow DOM, <= 30 KB gz.",
    "type": "module",
    "scripts": {
        "build": "node build.mjs",
        "size-check": "node size-check.mjs",
        "typecheck": "tsc --noEmit"
    },
    "dependencies": {
        "preact": "^10.24.0"
    },
    "devDependencies": {
        "esbuild": "^0.24.0",
        "typescript": "^5.6.0"
    }
}
